node_modules/
.DS_Store
Thumbs.db
*.log
*.css.map
.env

# Claude Code per-user IDE state — keep `launch.json` tracked, ignore the rest
.claude/settings.local.json

# Local scratch dir (image conversions, debugging assets)
tmp/
